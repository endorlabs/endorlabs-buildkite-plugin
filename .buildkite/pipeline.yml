steps:
  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.4.0:
        files:
          - hooks/**
          - lib/**
          - scripts/validation/render-matrix.sh

  - label: ":sparkles: Plugin linter"
    plugins:
      plugin-linter#v3.3.0:
        id: endorlabs
        image-version: v3.0.0
        readme: docs/examples.md

  - label: ":bash: BATS"
    plugins:
      docker-compose#v5.0.0:
        run: tests
        config: docker-compose.yml
